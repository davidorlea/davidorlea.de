{
  "name": "davidorlea.de",
  "version": "0.1.0",
  "description": "Personal Website",
  "repository": "https://github.com/davidorlea/davidorlea.de",
  "author": "David Orlea",
  "license": "UNLICENSED",
  "private": true,
  "source": "src/index.html",
  "engines": {
    "node": ">=22"
  },
  "browserslist": "> 0.5%, last 2 versions, not dead",
  "scripts": {
    "clean": "rm -rf build",
    "format": "prettier --write .",
    "prestart": "rm -rf build/dist",
    "start": "parcel serve --cache-dir build/cache --dist-dir build/dist --open",
    "prewatch": "rm -rf build/dist",
    "watch": "parcel watch --cache-dir build/cache --dist-dir build/dist",
    "prebuild": "rm -rf build/dist",
    "build": "parcel build --cache-dir build/cache --dist-dir build/dist --no-source-maps"
  },
  "devDependencies": {
    "@fontsource/open-sans": "^5.2.7",
    "@parcel/transformer-sass": "^2.12.0",
    "parcel": "^2.12.0",
    "prettier": "^3.8.3"
  }
}
